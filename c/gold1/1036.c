// 아직 c가 미숙해서 파이썬으로 다시 구현할 예정
#include <stdio.h>
#include <stdlib.h>

int cov36()

int main(void){
    int n, k;
    scanf("%d", &n);  // N입력
    int nums[n];
    for(int count=0; count<n; ++count){
        scanf("%d", &nums[count]);  // 36진수 수 입력
    }
    scanf("%d", &k);  // K입력
}